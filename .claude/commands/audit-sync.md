---
description: Compare l'état réel du code à sujet/lets-travel-audit.md et met à jour honnêtement les sections "Non implémenté / écarts" des READMEs
---

1. Relis `sujet/lets-travel-sujet.md` et `sujet/lets-travel-audit.md` (lecture
   seule — ne jamais les modifier, un hook bloque de toute façon toute édition
   sous `sujet/`).
2. Pour chaque point de la grille d'audit, vérifie **dans le code** (pas dans
   la documentation existante, qui peut être périmée) si c'est réellement
   implémenté, partiellement implémenté, ou absent.
3. Mets à jour honnêtement la section "Non implémenté / écarts avec le sujet"
   de `README.md`, ainsi que celle de `services/README.md` et du README du
   service concerné si elle existe — dans le même ton que l'existant : nommer
   ce qui manque plutôt que le passer sous silence, distinguer ce qui vient
   d'être corrigé de ce qui reste ouvert.
4. Ne reformule pas ce qui est déjà exact ; ne touche qu'aux sections qui ont
   réellement changé.
